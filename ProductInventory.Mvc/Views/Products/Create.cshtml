@model ProductInventory.Mvc.Models.Product
@{
    ViewData["Title"] = "Add Product";
}
<div class="container py-4">
    <h1 class="h4 mb-2 text-yellow">➕ Add Product</h1>
    <div class="text-muted-ink mb-3">Create a new item with a unique Product ID.</div>

    <div class="card-ui p-3">
        <div asp-validation-summary="ModelOnly" class="alert alert-danger mb-3"></div>

        <form asp-action="Create" method="post" class="row g-3">
            <div class="col-md-4">
                <label asp-for="ProductID" class="form-label">Product ID</label>
                <input asp-for="ProductID" class="form-control" placeholder="e.g. 201" autofocus />
                <span asp-validation-for="ProductID" class="text-warning small"></span>
            </div>

            <div class="col-md-5">
                <label asp-for="Name" class="form-label">Name</label>
                <input asp-for="Name" class="form-control" placeholder="e.g. Laptop" />
                <span asp-validation-for="Name" class="text-warning small"></span>
            </div>

            <div class="col-md-3">
                <label asp-for="Price" class="form-label">Price (R)</label>
                <div class="input-group">
                    <span class="input-group-text">R</span>
                    <input asp-for="Price" type="number" step="0.01" min="0" class="form-control" placeholder="0.00" />
                </div>
                <span asp-validation-for="Price" class="text-warning small"></span>
            </div>

            <div class="col-12 d-flex gap-2">
                <button type="submit" class="btn btn-green btn-pill">💾 Save</button>
                <a asp-action="Index" class="btn btn-orange-outline btn-pill">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
